// CVEHICLE.cpp
#include "CVEHICLE.h"

CVEHICLE::CVEHICLE(float startX, float startY, float spd, bool moveRight)
    : CObstacle(startX, startY, spd, moveRight)
{
    // Khởi tạo riêng cho phương tiện (nếu cần) đặt ở đây
}